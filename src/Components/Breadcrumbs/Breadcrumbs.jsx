import React from 'react';
import classNames from 'classnames';

export const Breadcrumbs = ({ className }) => {
    const componentClassNames = classNames('Breadcrumbs', className);

    return (
        <div className={componentClassNames}>
        </div>
    );
};
