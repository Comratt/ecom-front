import React from 'react';
import './GuestBtn.css';

const GuestBtn = () => (
    <div className="lib-guest-btn">
        <button>
            Continue as Guest
        </button>
    </div>
);

export default GuestBtn;
